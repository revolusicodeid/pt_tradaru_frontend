import React from 'react';

export const Result = () => (
    <div className="task-result" >
        <code className="text-result">Penggunaan dengan SQL database cukup membuat konsistensi data, karena relasional fiturnya</code>
    </div>
);

export default Result;